version: 2
updates:
  # Keep our own pinned third-party action SHAs fresh (this repo IS the reusable pipeline).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    groups:
      actions:
        patterns:
          - "*"
